# ── Core server ───────────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
websockets>=14.0
python-multipart>=0.0.20
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
loguru>=0.7.0
aiofiles>=24.1.0
httpx>=0.28.0            # async streaming client for vLLM REST API

# ── ASR — NVIDIA Parakeet RNNT 1.1B (NeMo) ───────────────────────────────────
nemo_toolkit[asr]>=2.2.0
torch>=2.5.0
torchaudio>=2.5.0

# ── LLM — Qwen3-32B-AWQ via vLLM ─────────────────────────────────────────────
# Install vLLM separately on the GPU instance:
#   pip install vllm>=0.6.6
# Launch command:
#   FLASHINFER_DISABLE_VERSION_CHECK=1 python -m vllm.entrypoints.openai.api_server \
#     --model Qwen/Qwen3-32B-AWQ --quantization awq \
#     --gpu-memory-utilization 0.55 --max-model-len 8192 --port 8001

# ── TTS — Kokoro-82M (self-hosted, no API key) ────────────────────────────────
kokoro>=0.9.4
soundfile>=0.12.0

# ── VAD — silero-vad v5 ───────────────────────────────────────────────────────
silero-vad>=5.1.2

# ── RAG — BGE-M3 embeddings + ChromaDB ───────────────────────────────────────
chromadb>=0.6.0
FlagEmbedding>=1.3.0

# ── Scraper (knowledge base builder) ─────────────────────────────────────────
beautifulsoup4>=4.12.0
lxml>=5.3.0
aiohttp>=3.11.0

# ── Audio utils ───────────────────────────────────────────────────────────────
numpy>=1.26.0

# ── Dev ───────────────────────────────────────────────────────────────────────
rich>=13.9.0
